<Window x:Class="Pharmacy321.ClientsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Pharmacy321"
        mc:Ignorable="d"
        Title="ClientsWindow" Height="450" Width="800">
    <Grid>
        <ListView ItemsSource="{Binding Clients}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding ID_Client}" />
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name_Client}" />
                    <GridViewColumn Header="Surname" DisplayMemberBinding="{Binding Surname_Client}" />
                    <GridViewColumn Header="Last Name" DisplayMemberBinding="{Binding LastName_Client}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
