<Window x:Class="Pharmacy321.EmployeesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Pharmacy321"
        mc:Ignorable="d"
        Title="EmployeesWindow" Height="450" Width="800">
    <Grid>
        <ListView ItemsSource="{Binding Employees}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding ID_Employee}" />
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name_Employee}" />
                    <GridViewColumn Header="Surname" DisplayMemberBinding="{Binding Surname_Employee}" />
                    <GridViewColumn Header="Last Name" DisplayMemberBinding="{Binding LastName_Employee}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
